# 实体领域候选匹配与模糊替换方案

## 1. 背景

当前项目已经支持用户维护标准实体、别名、实体类型、领域和规范化策略，并通过 `system/protection.py` 对转录文本中的实体进行保护和替换。

但是，当前实体匹配主要依赖精确字符串匹配。例如：

```text
标准实体：鬼灵门
ASR 输出：鬼灵们
```

如果用户没有提前把“鬼灵们”配置为“鬼灵门”的别名，系统就无法命中该实体，也无法完成正确替换。

这是因为 ASR 输出具有不确定性，用户不可能提前穷举所有潜在错误形式。因此，系统不能只依赖人工维护别名，而需要增加运行时的实体候选识别能力。

## 2. 核心目标

将当前的：

```text
标准名称/别名精确匹配
```

升级为：

```text
候选召回
    ↓
字符和拼音相似度计算
    ↓
领域和上下文重排序
    ↓
置信度决策
    ↓
自动替换、Refiner 提示或保留原文
```

系统需要同时满足以下要求：

1. 不要求用户提前知道 ASR 会产生什么错误形式。
2. 能够识别同音、近音、单字错误和轻微增删造成的实体变体。
3. 不因为领域词典存在就强行替换普通文本。
4. 高置信度候选可以自动规范化。
5. 中等置信度候选只作为 Refiner 的参考。
6. 低置信度或候选冲突时保留原始 ASR 文本。
7. 原始 ASR 文本、候选实体、评分和最终决策全部可追踪。

## 3. 当前实现状态

### 3.1 已有能力

当前仓库已经具备以下基础：

- `system/entity_store.py`：SQLite 术语库。
- `system/manage_entities.py`：命令行术语管理。
- `system/protection.py`：实体和数字等片段保护。
- `system/session_memory.py`：会话级实体记忆和可信度晋级。
- `system/web_app.py`：实体 API、领域选择和 WebSocket 转录。
- `system/web/index.html`：术语库管理面板和领域选择器。

当前术语实体已经包含：

```text
canonical_text
aliases
entity_type
domain
normalization_policy
priority
enabled
```

### 3.2 当前限制

当前 `EntityProtector` 主要通过精确文本查找实体，类似以下逻辑：

```text
标准实体：鬼灵门
文本中是否出现“鬼灵门”？
    是：命中
    否：不命中
```

因此，以下情况目前无法自动处理：

- 鬼灵门 → 鬼灵们
- Qwen3-ASR → Qwen ASR
- AgenticASR → Agentic SR
- 北京航空航天大学 → 北航大学
- 某个专业术语中出现一个同音错字

需要注意：当前领域选择器只负责选择领域词典，尚未完成基于领域和上下文的候选排序。

## 4. 总体设计

### 4.1 处理流程

推荐的完整流程如下：

```text
ASR 输出
  ↓
文本规范化
  ↓
领域实体词典过滤
  ↓
模糊候选召回
  ↓
字符相似度计算
  ↓
拼音相似度计算
  ↓
上下文领域相关性计算
  ↓
候选排序
  ↓
高置信度：自动保护和规范化
中置信度：作为 Refiner hint
低置信度：保留原文
```

### 4.2 领域的作用

领域不应该直接决定实体替换，而应该作为候选实体的先验信息。

例如，术语库中有：

```text
鬼灵门：game
鬼灵门：literature
```

当用户选择 `game` 领域，并且最近上下文包含：

```text
角色、装备、副本、技能、等级
```

那么游戏领域的“鬼灵门”候选应该得到更高分数。

但是，系统不能因为候选属于当前领域，就忽略字符和拼音差异直接替换。

最终替换必须同时考虑：

- 文本表面相似度。
- 拼音相似度。
- 当前领域匹配度。
- 最近上下文相关性。
- ASR 置信度。
- 候选之间的分数差距。

## 5. 领域词典设计

### 5.1 实体库记录

已有实体表可以继续使用：

```json
{
  "canonical_text": "鬼灵门",
  "entity_type": "TERM",
  "domain": "game",
  "normalization_policy": "normalize",
  "priority": 10,
  "aliases": [],
  "enabled": true
}
```

添加实体：

```bash
python -m system.manage_entities --db data/entities.db add 鬼灵门 \
  --type TERM \
  --domain game \
  --policy normalize \
  --priority 10
```

### 5.2 领域配置文件

建议新增：

```text
pipeline/configs/domains.json
```

第一版可以使用简单、可解释的关键词配置：

```json
{
  "general": {
    "keywords": [],
    "entity_types": ["TERM", "PROJECT", "MODEL"],
    "prior": 0.5
  },
  "game": {
    "keywords": [
      "角色",
      "装备",
      "副本",
      "技能",
      "等级",
      "任务",
      "地图",
      "公会"
    ],
    "entity_types": ["TERM", "CHARACTER", "ITEM", "SKILL", "LOCATION"],
    "prior": 1.0
  },
  "medical": {
    "keywords": [
      "患者",
      "诊断",
      "症状",
      "药物",
      "剂量",
      "治疗",
      "手术"
    ],
    "entity_types": ["DRUG", "DISEASE", "HOSPITAL", "PERSON"],
    "prior": 1.0
  }
}
```

### 5.3 领域实体加载规则

如果用户选择了 `game`：

```text
加载 game 领域实体
加载 general 通用实体
不加载 medical、legal 等其他领域实体
```

这部分可以复用当前 `EntityStore.list_entities(domain=...)` 的行为。

如果用户没有选择领域，则采用：

```text
只使用 general 实体自动替换
其他领域实体只作为低优先级候选
```

这样可以减少自动领域判断错误带来的实体误替换。

## 6. 领域上下文识别

### 6.1 第一阶段：用户手动选择

第一阶段建议直接使用 Web 页面中已有的领域选择器：

```text
用户选择领域 → 服务端加载对应领域词典
```

这是当前最可靠、最容易验证的方式。

### 6.2 第二阶段：关键词统计

系统维护最近几个稳定转录片段，例如最近 5～10 个片段：

```text
我们现在讨论游戏里的角色。
这个角色需要升级装备。
鬼灵们是一个比较重要的角色。
```

根据领域关键词统计领域分数：

```json
{
  "game": 0.91,
  "medical": 0.03,
  "legal": 0.02,
  "general": 0.50
}
```

建议使用滑动更新，避免一个错误片段导致领域突然切换：

```python
new_score = old_score * 0.8 + current_score * 0.2
```

### 6.3 第三阶段：领域分类模型

在数据积累后，可以增加：

- 轻量文本分类模型。
- Sentence embedding 与领域原型向量匹配。
- 基于开发集训练的领域分类器。

不建议第一版直接使用 LLM 判断领域，因为它会增加延迟，并且领域误判会影响所有实体候选。

## 7. 模糊实体候选匹配

### 7.1 匹配类型

候选匹配可以分为以下几类：

```text
EXACT              完全匹配
NORMALIZED         大小写、全角半角、标点规范化后匹配
CHAR_FUZZY         汉字字符相似或少量编辑距离
PINYIN_FUZZY       拼音相同或近似
CONTEXT_FUZZY      结合领域和上下文后匹配
```

### 7.2 “鬼灵们”示例

标准实体：

```text
鬼灵门
```

ASR 输出：

```text
鬼灵们
```

候选评分可以是：

```text
字符相似度：0.75
拼音相似度：1.00
领域匹配度：1.00
上下文相关性：0.92
```

候选结果：

```json
{
  "surface": "鬼灵们",
  "canonical": "鬼灵门",
  "entity_type": "TERM",
  "domain": "game",
  "match_type": "PINYIN_FUZZY",
  "score": 0.93
}
```

### 7.3 综合评分

初始版本可以采用以下公式：

```text
final_score =
    0.50 × text_similarity
  + 0.25 × domain_score
  + 0.15 × context_score
  + 0.10 × asr_confidence
```

当 ASR 没有提供置信度时，不能伪造该字段，可以：

- 将该项从计算中移除并重新归一化；或者
- 降低自动替换上限，只允许作为 Refiner hint。

以上权重只是初始值，最终应该通过开发集进行调优。

## 8. 候选决策策略

不能只看最高候选分数，还要看它是否明显领先于第二候选。

推荐初始策略：

```text
best_score >= 0.90
且 best_score - second_score >= 0.10
    → 自动替换

0.75 <= best_score < 0.90
或候选分差不足
    → 只作为 Refiner hint

best_score < 0.75
    → 保留原文
```

高风险实体应使用更高阈值：

```text
人名、药物、金额、法律条款、身份证号
    → 自动替换阈值提高到 0.95 或要求用户确认
```

示例：

```text
候选 A：鬼灵门，0.93
候选 B：鬼灵们，0.81
```

可以自动替换。

但如果：

```text
候选 A：鬼灵门，0.88
候选 B：鬼灵们，0.86
```

候选过于接近，则保留 ASR 原文，并把候选传给 Refiner 或展示给用户。

## 9. 推荐新增模块

建议新增：

```text
system/entity_matcher.py
system/domain_context.py
pipeline/configs/domains.json
```

### 9.1 `system/domain_context.py`

负责维护会话级领域状态：

```python
@dataclass
class SessionDomainState:
    scores: dict[str, float]
    selected_domain: str | None = None
    locked_domain: str | None = None
```

核心职责：

- 接收最近的稳定转录文本。
- 统计各领域关键词。
- 更新领域分数。
- 返回当前领域排序。
- 防止领域在短时间内频繁切换。

### 9.2 `system/entity_matcher.py`

负责实体候选召回和评分：

```python
@dataclass(frozen=True)
class EntityCandidate:
    surface: str
    canonical: str
    entity_type: str
    domain: str
    char_score: float
    pinyin_score: float
    domain_score: float
    context_score: float
    final_score: float
    match_type: str
    decision: str
```

建议接口：

```python
class EntityCandidateMatcher:
    def find_candidates(
        self,
        text: str,
        entities: tuple[EntityDefinition, ...],
        domain_scores: dict[str, float],
        asr_confidence: float | None = None,
    ) -> tuple[EntityCandidate, ...]:
        ...
```

### 9.3 修改 `EntityProtector`

当前 `EntityProtector` 负责保护实体和恢复占位符，后续应改为：

```python
candidate = matcher.best_candidate(...)

if candidate.decision == "AUTO_NORMALIZE":
    # 保护并规范化为标准实体
elif candidate.decision == "HINT_ONLY":
    # 不直接替换，只向 Refiner 提供标准名称
else:
    # 保留原始 ASR 文本
```

`ProtectedSpan` 建议增加：

```python
match_type: str
match_score: float
decision: str
```

## 10. 与 Refiner 的结合方式

推荐保留两类信息：

### 高置信度候选

进入保护和恢复流程：

```text
鬼灵们 → ⟦P000⟧
```

Refiner 处理后恢复为：

```text
鬼灵门
```

### 中置信度候选

不进行强制替换，只作为 Refiner hint：

```text
原文：鬼灵们
候选：鬼灵门
```

Refiner 可以结合整句上下文决定是否使用候选。如果结果仍然不确定，则保留原文。

### 低置信度候选

不传给 Refiner，也不修改文本，只写入调试日志：

```json
{
  "surface": "鬼灵们",
  "candidate": "鬼灵门",
  "score": 0.61,
  "decision": "KEEP_RAW"
}
```

## 11. 会话记忆规则

当前 `SessionEntityMemory` 可以继续使用，但应区分以下来源：

```text
数据库验证实体
ASR 多次观察到的候选
Refiner 推测出的实体
用户确认的实体
```

建议规则：

- 数据库实体立即视为 `VERIFIED`。
- Refiner 单次输出不能写入永久术语库。
- 同一候选需要多个独立稳定片段和高 ASR 置信度才能晋级为会话级 `ACCEPTED`。
- 用户确认后才写回 SQLite。
- 不同候选分数接近时禁止自动晋级。
- 重复 partial 不计为独立证据。

## 12. 日志和 Web 展示

每次候选匹配都建议记录：

```json
{
  "surface": "鬼灵们",
  "canonical": "鬼灵门",
  "domain": "game",
  "match_type": "PINYIN_FUZZY",
  "char_score": 0.75,
  "pinyin_score": 1.0,
  "domain_score": 1.0,
  "context_score": 0.92,
  "final_score": 0.93,
  "decision": "AUTO_NORMALIZE",
  "accepted": true
}
```

前端可以在精修历史中显示：

```text
实体候选：鬼灵们 → 鬼灵门
匹配方式：拼音模糊匹配
领域：游戏
置信度：0.93
结果：已自动规范化
```

中等置信度候选则显示：

```text
候选实体：鬼灵门
当前未自动替换，已作为精修参考
```

## 13. 测试计划

### 13.1 候选匹配测试

至少覆盖：

```text
鬼灵们 → 鬼灵门
Qwen ASR → Qwen3-ASR
Agentic SR → AgenticASR
大小写差异
全角半角差异
中英文混合实体
一个字符增删
一个同音字替换
```

### 13.2 领域测试

覆盖以下情况：

1. 当前领域实体优先于其他领域实体。
2. `general` 实体在所有领域可用。
3. 未选择领域时不自动使用高风险专业实体。
4. 上下文关键词能够提高相关领域候选分数。
5. 单个错误片段不会导致领域突然切换。
6. 候选分数接近时不会强行替换。

### 13.3 安全测试

覆盖：

- 模糊候选错误时回退原始文本。
- 高风险实体使用更高阈值。
- Refiner 输出未知候选时拒绝。
- Refiner 删除保护实体时拒绝。
- 候选不会因为一次 Refiner 输出自动写入永久术语库。

## 14. 实施顺序

建议按照以下顺序落地：

1. 保留当前 Web 手动领域选择功能。
2. 新增中文文本规范化和拼音计算工具。
3. 新增字符相似度和拼音相似度候选匹配。
4. 在候选评分中加入领域分数和实体优先级。
5. 将高置信度候选接入 `EntityProtector`。
6. 将中置信度候选作为 Refiner hint。
7. 增加候选分数、匹配原因和决策日志。
8. 增加 Web 候选结果展示。
9. 使用最近 5～10 个稳定片段实现上下文领域评分。
10. 最后再实现自动领域识别和领域分类模型。

## 15. 验收标准

完成第一版后，需要满足：

1. 对“鬼灵们 → 鬼灵门”这类同音错字能够召回候选。
2. 不需要提前把“鬼灵们”设置为别名。
3. 领域选择能够影响候选排序。
4. 领域不匹配时不会因为拼音相似就强行替换。
5. 高置信度候选可以自动规范化。
6. 中置信度候选只作为 Refiner 参考。
7. 低置信度候选保持原文。
8. 原始文本、候选实体、评分、领域和决策全部进入 JSONL 日志。
9. 所有保护失败都能回退到原始 ASR 文本。
10. 候选匹配不会自动污染永久术语库。

## 16. 最终原则

领域候选不是“只要属于当前领域就替换”，而是：

> 使用领域信息缩小候选范围并调整候选排序，再结合字符、拼音、上下文和 ASR 置信度进行安全决策。

对于“鬼灵门”这类实体，术语库只需要保存标准名称和领域；“鬼灵们”这样的 ASR 错误形式由运行时模糊匹配动态发现，不需要用户提前穷举所有别名。
